import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
      title: "Hello, Flutter!",
      home: Scaffold(
        appBar: AppBar(title: const Text("Hello Flutter")),
        body: const Text("플러터 실험 중"),
      ),
    ));
